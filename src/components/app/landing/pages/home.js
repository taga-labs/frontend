import Nav from '../../common/LandingNav';

export default function Home(props) {
    const [] = useState();

    return(
        <div className="container h-screen w-screen bg-slate-900 mx-auto">
            <Nav />
        </div>
    )
}